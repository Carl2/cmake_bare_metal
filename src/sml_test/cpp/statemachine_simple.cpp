#include "statemachine_simple.hpp"

void start_simple_state_machine(UART_HandleTypeDef& huart)
{

    // bl_sm.process_event(evStartBlink{});
    // Do something with the state_machine
}

void timer_interrupt()
{
    // Do something during timer interrupt
}
